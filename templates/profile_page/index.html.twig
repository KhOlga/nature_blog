{% extends 'base.html.twig' %}

{% block title %}Hello {{ controller_name }}!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ 'src/Controller/ProfilePageController.php'|file_link(0) }}">src/Controller/ProfilePageController.php</a></code></li>
        <li>Your template at <code><a href="{{ 'templates/profile_page/index.html.twig'|file_link(0) }}">templates/profile_page/index.html.twig</a></code></li>
    </ul>

    <div class="col-sm-12 col-md-8">
        Here will be form
        <h1>Your lucky number is {{ number }}</h1>

        {% for i in 1..10 %}
            <div class="{{ cycle(['even', 'odd'], i) }}">
                <!-- some HTML here -->
                <h2>Test number is {{ number }}</h2>
            </div>
        {% endfor %}


    </div>
</div>
    </ul>

    <div class="col-sm-12 col-md-8">
        Here will be form
        <h1>Your lucky number is {{ number }}</h1>

        {% for i in 1..10 %}
            <div class="{{ cycle(['even', 'odd'], i) }}">
                <!-- some HTML here -->
                <h2>Test number is {{ number }}</h2>
            </div>
        {% endfor %}


    </div>



</div>


    </ul>


</div>
    <div>
        new text for test
        <div class="col-sm-12 col-md-8">
            Here will be form
            <h1>Your lucky number is {{ number }}</h1>
        </div>

    </div>

{% endblock %}
